export const tagType = {
  product: "product",
  user: "user",
  order: "order",
  dashboardStats: "dashboardStats"
}
export const tagProvider = [tagType.product, tagType.order, tagType.user, tagType.dashboardStats]